diff --git a/contracts/UniswapV2.sol b/contracts/UniswapV2.sol
index e38458b5..82f0419d 100644
--- a/contracts/UniswapV2.sol
+++ b/contracts/UniswapV2.sol
@@ -42,10 +42,11 @@ contract UniswapV2 is ERC20("Uniswap V2", "UNI-V2", 18, 0) {
         reentrancyLock = false;
     }
 
-    constructor() public {
+    constructor(address _tokenA, address _tokenB) public {
+        require(address(_tokenA) != address(0) && _tokenB != address(0), "Uniswap: INVALID_ADDRESS");
         factory = msg.sender;
-        // tokenA = _tokenA;
-        // tokenB = _tokenB;
+        tokenA = _tokenA;
+        tokenB = _tokenB;
     }
 
     function () external {}
