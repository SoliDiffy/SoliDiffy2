diff --git a/contracts/interfaces/IUniswapV3Pair.sol b/contracts/interfaces/IUniswapV3Pair.sol
index 9a5b1bb1..3e91ce3e 100644
--- a/contracts/interfaces/IUniswapV3Pair.sol
+++ b/contracts/interfaces/IUniswapV3Pair.sol
@@ -79,12 +79,30 @@ interface IUniswapV3Pair {
         uint128 amount
     ) external returns (uint256 amount0, uint256 amount1);
 
-    function swap(
-        bool zeroForOne,
-        int256 amountSpecified,
+    // swapping
+    function swapExact0For1(
+        uint256 amount0In,
         address recipient,
         bytes calldata data
-    ) external returns (uint256 amountCalculated);
+    ) external returns (uint256 amount1Out);
+
+    function swap0ForExact1(
+        uint256 amount1Out,
+        address recipient,
+        bytes calldata data
+    ) external returns (uint256 amount0In);
+
+    function swapExact1For0(
+        uint256 amount1In,
+        address recipient,
+        bytes calldata data
+    ) external returns (uint256 amount0Out);
+
+    function swap1ForExact0(
+        uint256 amount0Out,
+        address recipient,
+        bytes calldata data
+    ) external returns (uint256 amount1In);
 
     function setFeeTo(address) external;
 
