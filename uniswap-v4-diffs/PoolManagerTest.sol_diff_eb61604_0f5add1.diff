diff --git a/contracts/test/PoolManagerTest.sol b/contracts/test/PoolManagerTest.sol
index 8cdebabd..caad41d7 100644
--- a/contracts/test/PoolManagerTest.sol
+++ b/contracts/test/PoolManagerTest.sol
@@ -1,13 +1,13 @@
 // SPDX-License-Identifier: UNLICENSED
 pragma solidity =0.8.19;
 
-import {IERC20Minimal} from "../interfaces/external/IERC20Minimal.sol";
-import {IPoolManager} from "../interfaces/IPoolManager.sol";
-import {ILockCallback} from "../interfaces/callback/ILockCallback.sol";
+import {IERC20Minimal} from '../interfaces/external/IERC20Minimal.sol';
+import {IPoolManager} from '../interfaces/IPoolManager.sol';
+import {ILockCallback} from '../interfaces/callback/ILockCallback.sol';
 
 contract PoolManagerTest is ILockCallback {
     function lock(IPoolManager manager) external {
-        manager.lock("");
+        manager.lock('');
     }
 
     /// @notice Called by the pool manager on `msg.sender` when a lock is acquired
